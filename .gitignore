# OS
.DS_Store

# 环境变量(含 API Key,绝不入库)
.env
.env.*

# 数据库文件(含 WAL/SHM 副本);schema.sql/seed.sql 保留入库,运行时 .db 忽略
data/*.db
data/*.db-*
db/*.db
db/*.db-*

# 构建产物 / 依赖
node_modules/
dist/
build/
target/

# Playwright
frontend/test-results/
frontend/playwright-report/
frontend/.playwright/
